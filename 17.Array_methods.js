// Array methods 

// Array methods (convert the value in string)//
let num = [1, 2, 3, 34, 4];
// let b = num.toString(); // b is now a string //
// console.log(b, typeof b);

// let c = num.join(" and "); // this method is join saprate to Array //
// console.log(c, typeof c);

// ----------------------------------------------------------------------------------------------------//

// let r = num.pop(); // pop remove the last element of the array.(pop out karna), mens last element ko remove kar deta hai. //
// console.log(num, r);

// ----------------------------------------------------------------------------------------------------//

// let p = num.push(83,100); // push returns the new array length, push method Array me last new length add kartha hai//
// console.log(num, p);

// ----------------------------------------------------------------------------------------------------//

// let d = num.shift() // Removes an elements from the start of the array, Array ke first elements ko delet kartha hai //
// console.log(d, num);

// ----------------------------------------------------------------------------------------------------//

let e = num.unshift(78,25,100); // Adds an elements to the start of the array, Array ke first me new elements add kartha hai //
console.log( num); 

// ----------------------------------------------------------------------------------------------------//

